CREATE TABLE IF NOT EXISTS operation (
   id BIGINT NOT NULL AUTO_INCREMENT,
   name      VARCHAR(100) NOT NULL ,
   standard  INT,
   ratio     DOUBLE,
   technology_id BIGINT,
   PRIMARY KEY(id),
   FOREIGN KEY (technology_id) references ebdb.technology(id)
);